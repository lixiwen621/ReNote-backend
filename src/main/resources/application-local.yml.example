# 本地个人覆盖（不提交真实密钥）
# 使用方式：
#   1. 复制本文件为同目录下的 application-local.yml（该文件名已加入 .gitignore）
#   2. 启动时激活 profile：--spring.profiles.active=local
#   3. 若需「先 prod 再本地覆盖」：--spring.profiles.active=prod,local（后者覆盖前者同名项）
#
# 合并顺序：application.yml → application-<profile>.yml（多个 profile 按从左到右，右侧覆盖左侧）

# server:
#   port: 8084

# spring:
#   datasource:
#     url: jdbc:mysql://127.0.0.1:3306/renote?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
#     username: root
#     password: ${DB_PASSWORD}

# jwt:
#   secret: ${JWT_SECRET}

# logging:
#   level:
#     com.renote.backend.mapper: debug
